using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Randomer 
{
   /* private double born;
    private System.Random random;
    private Character characterMe;
    private Character characterAdv;
    
    double[] a;
    public Randomer(CharacterRender render) {
        born = 0;
        random = new System.Random();
        a = new double[4];
    }

    public int interact(Character charAdv,Character charMe){
        
        int k = 0;
        /*if(internalHorloge > born)
        {
            born = random.NextDouble() * 2.0d + 1d;
            internalHorloge = 0;
           
            if(characterMe != null
               && characterAdv != null)
            {
                charAdv.SetBombe();
            }
            
            for(k = 0; k < 4; k++)
                a[k] = random.NextDouble();


            
        }
        if(charAdv != null){
            
        }
        return 0;
    }
    */
}
